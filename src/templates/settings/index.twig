{% extends "_layouts/cp" %}

{% set selectedSubnavItem = 'settings' %}
{% set title = "translate.settings.title" | t('eventsky') %}

{# {% import "_includes/forms" as forms %}

{{ forms.textField({
    first: true,
    label: "Url",
    name: 'url',
    value: settings.url
}) }} #}

{# {{ forms.textField({
    label: "Bar",
    name: 'bar',
    value: settings.bar
}) }} #}

{# {{' TEST TEST TEST' }} #}