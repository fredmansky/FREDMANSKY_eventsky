{% import "_includes/forms" as forms %}

{{ forms.selectField({
    label: 'translate.ticket.fields.details.event' | t('eventsky'),
    id: 'eventId',
    name: 'eventId',
    value: ticket.eventId,
    errors: ticket.getErrors('eventId'),
    required: true,
    options: ticketEventOptions
}) }}

{{ forms.textField({
    label: 'translate.ticket.fields.details.email' | t('eventsky'),
    id: 'email',
    name: 'email',
    value: ticket.email,
    errors: ticket.getErrors('email'),
    required: true,
}) }}